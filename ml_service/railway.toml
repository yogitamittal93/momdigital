[build]
dockerfilePath = "ml_service/Dockerfile"
buildContext = "ml_service"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
